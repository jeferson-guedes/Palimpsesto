# Palimpsesto ships only the structural layer — never commit real memory here.
memory/
*.local.md
.DS_Store

# Semantic layer: local databases, venv, and domain overrides are yours, never committed.
semantic/data/
semantic/.venv/
semantic/.last_maintenance
semantic/tags.local.json
__pycache__/
*.pyc
